module.exports.hooks = {
  // Include core hooks (optional if you want to specify or disable them)
  // e.g., 'orm': true,

  // Include custom hooks
  "auto-reload": true,
  nextjs: true,

  // Exclude hooks (optional)
  // e.g., 'blueprints': false,
};
